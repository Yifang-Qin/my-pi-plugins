# deps installed by pi via npm; never commit
node_modules/
npm-debug.log*

# pi runtime state (should never be in a config repo)
auth.json
sessions/
tmp/

# afang-subagent background task reports
.pi/subagent-results/

# private local dev notes (per-machine dev workflow, symlink list)
AGENTS.md

# OS junk
.DS_Store
